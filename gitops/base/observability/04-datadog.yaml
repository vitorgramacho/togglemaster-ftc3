# =============================================================================
# ArgoCD Application: Datadog Agent (Versão Otimizada AWS Academy)
# ---------------------------------------------------------------------------
# Redução severa nos requests de memória do DaemonSet e Cluster Agent para
# evitar sufocar nós t3.medium. Preserva K8s Live, Process Map e APM via OTLP.
# =============================================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: datadog
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://helm.datadoghq.com
    chart: datadog
    targetRevision: 3.81.0
    helm:
      releaseName: datadog
      values: |
        # ---------- Identificação ----------
        datadog:
          apiKeyExistingSecret: datadog-secret
          site: us5.datadoghq.com
          clusterName: togglemaster-eks
          tags:
            - "env:production"
            - "project:togglemaster"
            - "phase:4"

          # ---------- APM ----------
          apm:
            portEnabled: true
            socketEnabled: true
            instrumentation:
              enabled: false   # auto-injection desligada

          # ---------- Logs ----------
          logs:
            enabled: false

          # ---------- Process Agent ----------
          processAgent:
            enabled: true
            processCollection: true

          # ---------- System Probe (Garante que eBPF/NPM fiquem desligados) ----------
          systemProbe:
            enabled: false

          # ---------- Métricas extras ----------
          dogstatsd:
            originDetection: true
            useHostPort: true
          orchestratorExplorer:
            enabled: true        # mostra deployments/HPAs no Datadog
          kubeStateMetricsEnabled: false   # já vem do kube-prometheus-stack

        # ---------- Cluster Agent ----------
        clusterAgent:
          enabled: true
          replicas: 1
          metricsProvider:
            enabled: false     # HPA continua usando o metrics-server padrão
          resources:
            requests: { cpu: 20m, memory: 128Mi }
            limits:   { cpu: 250m, memory: 256Mi }

        # ---------- Node Agent (DaemonSet) ----------
        agents:
          containers:
            agent:
              resources:
                requests: { cpu: 50m, memory: 128Mi }
                limits:   { cpu: 300m, memory: 256Mi }
            traceAgent:
              resources:
                requests: { cpu: 20m, memory: 64Mi }
                limits:   { cpu: 150m, memory: 128Mi }
            processAgent:
              resources:
                requests: { cpu: 20m, memory: 64Mi }
                limits:   { cpu: 150m, memory: 128Mi }
  destination:
    server: https://kubernetes.default.svc
    namespace: observability
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m